<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}GamePoint{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-md navbar-dark bg-dark border-bottom shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-semibold" href="{{ path('app_home') }}">GamePoint</a>
        <div class="ms-auto d-flex flex-wrap align-items-center gap-2 py-1" role="navigation" aria-label="Kimlik doğrulama">
            {% if app.user %}
                <span class="text-white-50 small">{{ app.user.userIdentifier }}</span>
                <a class="btn btn-outline-light btn-sm" href="{{ path('app_logout') }}">Çıkış</a>
            {% else %}
                <a class="btn btn-outline-light btn-sm" href="{{ path('app_login') }}">Giriş</a>
                <a class="btn btn-primary btn-sm" href="{{ path('app_register') }}">Kayıt ol</a>
            {% endif %}
        </div>
    </div>
</nav>
{% block body %}{% endblock %}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
